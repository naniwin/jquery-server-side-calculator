// requires 
const express = require('express');
const app = express();
// global port to use
const 5000;
// spin up server
app.listen(port,() =>{
    console.log('server up on: ', port);
});
// GET route from history to display